# Port the relay server listens on
PORT=5000

# Comma-separated list of allowed browser origins (defaults to all origins).
# Only needed when the frontend is served from a different host than the relay.
ORIGIN=*

# Folder containing the built frontend (defaults to ../dist)
STATIC_DIR=../dist